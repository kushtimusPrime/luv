dataset:
  name: "FCDataset"
  dataset_dir: "data/cable_seg"
  val: False
  transform: True
dataset_val:
  name: "FCDataset"
  dataset_dir: "data/cable_seg"
  val: True
  transform: True
train:
  loss:
    name: "SegmentationLoss"
  optimizer:
    optim_learning_rate: 0.002
  num_classes: 1
  seed: -1
  loader_n_workers: 4
  n_gpus: 1
  epochs: 200
experiment: "cable_segmentation"
